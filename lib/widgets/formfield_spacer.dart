import 'package:flutter/material.dart';

class FormFieldSpacer extends StatelessWidget {

  const FormFieldSpacer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 30.0);
  }
}
